<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="min-h-screen w-full bg-zinc-900 text-white p-10">
        <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block">Logout</a>
        <h3 class="text-3xl mb-5">Hello , <%= user.name%></h3>
        <h5 class="mb-5">you can create a post</h5>
        <form action="/post" method="post">
            <textarea name="" class="block w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-lg p-3" placeholder="what's on your mind ?" name="content"></textarea>
            <input type="submit" value="Create New Post" class="block px-3 py-2 bg-blue-500 rounded-md mt-5 w-50 text-md">
        </form>

        <div class="posts mt-10">
            <h3 class="text-zinc-400">Your Posts </h3>
            <div class="postcontainer mt-4">
                <% user.posts.forEach(function(post){ %>
                         <div class="post w-1/3 p-4 rounded-md mb-2 bg-zinc-800">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p><%=post.content %></p>
                    <div class="btns flex gap-4 mt-2">
                        <a href="" class="text-blue-500">Like</a>
                        <a href="" class="text-zinc-600">Edit</a>
                    </div>
                </div>
                <% }) %>

            </div>
        </div>
    </div>

</body>
</html>